projection;
strict ( 1 );
use draft;

define behavior for ZC_INVOICE alias Invoice
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}